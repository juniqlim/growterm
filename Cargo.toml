[workspace]
# growterm-integration-tests is a member so `cargo test` keeps it compiling.
# Its app-launching tests are #[ignore]d, so they stay opt-in:
# `cargo test -- --ignored`.
members = ["growterm-*"]
resolver = "2"
